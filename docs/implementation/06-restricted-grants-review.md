# 06 双轴代码审查

以本次实施前的 `37a51da275d4d5cbad2e6edc4fdb5ff4fb05389b` 为基点，初次审查 `git diff 37a51da...76970d6`，修复后复核范围为 `37a51da...48d51a1`。Standards 与 Spec 由两个独立审查者并行只读执行。

Spec 来源为 [06 票及 Agent Brief](../../.scratch/harness-implementation/issues/06-grants.md)、共享实施规格及授权生命周期分册。Standards 核对 AGENTS.md、CONTEXT.md、docs/agents 约定、共享实施规格与 code-review 的代码异味基线。

## Standards

文档硬规则违规 0 项；初次 1 项 P3 判断性建议：父子授权收缩判断在派生接纳与祖先链核验中重复，新增限制容易产生语义漂移。

修复以私有 GrantAuthority.attenuates 统一目标范围、委派深度、时间、额度及资源范围比较。派生接纳单独检查剩余额度，既有链核验不重复扣算分配。原审查者确认 P3 关闭；新增密钥检查和验证夹具没有新的可行动问题。

## Spec

初次 2 项 P2：

- 必需密钥映射缺失或与私钥不匹配仍可启用签发，可能交付本实现无法核验的材料。构造器现拒绝空映射、缺少签发 kid、无效曲线/点和公私钥不匹配，并复制密钥配置。四种配置失败均有实际拒绝测试。
- 委派扩权负例被更早的窄策略/TTL 上限拒绝，且原并发检查只覆盖 ReserveUse，没有独立验证签名期间父撤销和并发 DERIVE。现使用宽策略和窄父授权验证逐维收缩，补充注册资源包含负例、两个 SQLite 句柄上的父撤销签名竞态，以及双签名屏障下的派生竞争。检查未交付失效材料、未保留部分预留及未超额分配。

原审查者复核确认 2 项 P2 均已关闭，未发现新增问题或范围膨胀。

Standards：累计 1 项 P3，关闭 1 项，未解决 0 项；Spec：累计 2 项 P2，关闭 2 项，未解决 0 项。最终 `48d51a1` 通过 [完整验证](evidence/06-verification-stages.json)，构建时工作区干净。
