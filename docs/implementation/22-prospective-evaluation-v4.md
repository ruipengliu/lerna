# 22 票真实模型评价 v4

计划 search-small-prospective-v4，执行前登记。沿用 [v3](22-prospective-evaluation-v3.md) 的固定材料、顺序、逐例标准与预算。v2 的 0/8 和 v3 的语义 6/8 保留；本轮是已知材料修复回归，不是盲测或泛化正确率。

本轮修复获取失败时的输入范围：通过原任务当前授权和计量路径提供实际搜索发现记录，仍不将搜索摘要提升为网页证据。提示要求 scope 表达实际来源可用性，仅在输入明确包含发现记录时声称已观察索引，不把任务截止时间转换为证据日期。模型能力版本为 ark-chat-v3-20260910-citations-v3。

先 loopback 四例，再 replay 四例，每例一次真实模型调用，最多 8 次，无自动重试或替换。每次输出最多 512 token，API 20 秒，每例 30 秒；模型预算 262144 token、查询 128、搜索 4096 字节、页面 1024 字节。必要事实、引用与范围标准不变，生成后独立语义审查，不以运行发布成功替代质量通过。

本轮预留 8 元管理额度，此前两轮合计 16 元预留保留；预留不是实际账单。遵守用户开发阶段累计模型费用低于 500 元授权，记录所有实际 token 和未知用量。公网完整链路另行验证。

执行代码及文件摘要见 execution-lock.json。修复已通过实际任务输入回归、Ark/Brain race 和相关 vet；Spec/Standards 增量审查无剩余发现。旧版本全仓验证不作为本轮代码的完整证明。
