# 14 票双轴代码审查

比较基点 `49b27dc5c066ead0e3f6dc0b3ccf2b5960e7dec8`，固定命令 `git diff 49b27dc...167051f`。审查提交 `5e15924`、`167051f`。两轴由独立代理执行，修复后分别复核；规格来源是 14 票已确认 Agent Brief 和架构的大脑分册。规范来源为 AGENTS.md、docs/agents 三份约定及 CONTEXT.md，并采用技能规定的十二项代码味道基线。

## Standards

初审没有仓库文档规范违例，提出两项 P3 判断建议：调用绑定传递连续同类型参数，三个能力选择循环重复且过滤不一致。

`167051f` 引入 `tasks.ActionBinding` 并使用具名字段；`scriptCapability` 统一角色过滤、解析和摘要选择。独立复核确认两项建议均已解决。新增恢复逻辑仍由 Core 管理任务状态和 Worker 资格，Execution 保留调用身份；操作身份预留不授予执行权限，交互复用既有等待契约。未发现新增或剩余明确规范违例或代码味道。

剩余：0 项硬性违例，0 项建议。

## Spec

初审有 3 项 P1 和 1 项 P2：派发落盘但 Invocation 尚未建立时恢复停滞；过期租约无重取入口；已保存提案恢复时缺少来源复核；缺参数只进入 WAITING 而没有可回复的交互项。

`167051f` 增加 EnsureLease，递增 Worker generation 并保留原 Invocation 资格与请求指纹；独立的当前宿主资格绑定仍在实际准入和启动时检查控制、期限及原操作。派发后的权威查询确认尚未启动时重放原请求，已启动未知效果只核对。恢复准入和新启动复核来源；标准 Pending Interaction 保存具体问题，ProvideInput 后继续执行。预留执行域身份使提案可跨签发窗口恢复，尚未建立 Invocation 的许可分配也沿用原记录，并再次 ValidateUse。

独立复核运行 tasks、authorization、brain、asynccheck 和 catalogcheck 包测试，确认原四项问题已闭环，没有新增实质缺陷或范围扩张。execution 包的行为由 profile 测试覆盖，没有独立包测试。

剩余代码发现：0 项。该轮审查时真实付费模型为 `not_run`，当时该验收项尚未完成，不能从静态复审或脚本测试推导质量通过。

该轮代码发现数量：Standards 0，Spec 0；当时的真实模型验收缺口独立保留。全量运行证据见实施指南。

## 真实回归修复的增量复审

整体基点仍为 49b27dc；增量命令 git diff 2ea05f3...ddc6903。真实回归发现 InputRefs 仅被验证、未交付模型，另有无必要等待首次正确标记不实；两个测试先失败，修复后通过。原失败报告和公开输入输出保留。

Standards 独立复审：0 项规范违例、0 项明确 smell。输入正文沿用受控读取和来源信息，两项测试检查外部行为。

Spec 独立复审：0 项新增实质缺陷或范围扩张。来源与模型位置校验先于正文读取，失败停止组装；不必要等待被标记为首次失败，原始决策保留。独立重跑两项新测试及云授权测试通过。

两轴各 0 项剩余代码发现。此前 not_run 是当时状态，最终真实回归结果及限制见实施指南，不能从审查推导统计质量。
