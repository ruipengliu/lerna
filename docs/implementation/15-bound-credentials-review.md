# 15 票双轴代码审查

比较基点为 d7592ad，初审候选 511d45f，修复后候选 f5df39f。固定比较命令为 git diff d7592ad...f5df39f。两轴由独立代理执行并分别复核。规格来源为 15 票 Agent Brief、工作包共同约束和授权生命周期分册；规范来源为 AGENTS.md、docs/agents 约定、CONTEXT.md 及技能规定的十二项代码味道基线。

## Standards

初审没有硬性规范违例，有一项可选 P3：Put、Rewrap 和 CLI 的元数据出口处理重复。修复将再次授权与持久化集中到 Broker.commit，Record.Metadata 明确列出允许公开的字段，CLI 复用相同投影。

复审确认原建议关闭，新增超时处理及恢复测试未发现新的文档规范违例或值得报告的代码味道。

剩余：0 项硬违例，0 项建议。

## Spec

初审两项 P2：stdin 仅限制字节数，停滞管道可能无限阻塞；轮换中断和数据库旧快照恢复缺少实际证据。

readSecret 现在响应 context 截止时间，取消时关闭输入并清理迟到内容，固定错误不携带秘密。真实停滞 stdin 子进程测试先因被超时终止而失败，修复后按截止时间返回，确认未保存凭证。

部分 rewrap 测试使用真实 SQLite 与独立密钥目录，子进程仅迁移第一条记录后直接退出，父进程重开后验证两代密钥下的记录可调用、继续迁移及独立目标效果。旧数据库恢复测试保持密钥目录不回滚，验证同一密钥的 nonce 不复用，并在受信恢复处理后验证调用。

复审确认两项问题关闭，无新增规格问题。管理投影保留绑定、授权复查和密文隔离语义。复审已核对定向 race 日志；全量验收以实施指南中的最终归档结果为准。

剩余：0 项。Standards 和 Spec 各自均无剩余发现，代码审查不替代运行证据。
