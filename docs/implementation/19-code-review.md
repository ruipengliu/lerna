# 19 票双轴审查

固定比较：`git diff fee7662...HEAD`。首次审查终点为 `3b576b0`；需求来源 `.scratch/harness-implementation/issues/19-deletion.md` 及共享规格。Standards 和 Spec 由两个独立代理并行审查，未由代理运行完整测试。

## Standards

初次审查未发现违反 AGENTS.md、其引用规范或 CONTEXT.md 的明确硬违规。以下均为判断性维护建议，最高 P3：

- **可能的 Duplicated Code**：`adapters/context/sqlite/invalidation.go` 与 `retirement.go` 重复维护退役身份、清空主体/比较/正文和删除 checkpoint pin；建议共用私有事务函数。已提取 `retireSnapshot`，各入口保留容量检查、来源水位和提交责任；真实 SQLiteContext/MemoryCleanup 完整 race 通过。
- **可能的 Refused Bequest**：恢复视图为适配既有 QueryStore，提供三个恒返回 Quarantined 的写入/操作历史方法。建议拆分 Reader 所需接口。此项暂不扩展公共构造接口：当前 Reader 构造仍依赖 Service/QueryStore，而恢复视图明确关闭修改权威，SDK 真实测试已验证。保留这一 P3 接口改进建议，不把它描述为硬违规或数据权限缺陷。

## Spec

初次审查发现一项 P2 部分需求：Brief 要求“通过消费方接口登记清理责任及完成位置”，并在失败/退出后“从原变更位置继续”。已有旧检查点周期迁移 Job 没有持久消费者确认，报告永久 nil Binding；现有受控运行材料不能使用未接入远端/归档的豁免关闭验收。

修复接入 Checkpoints 来源消费者：固定文件/任务范围配置绑定、实际维护与只读确认后才逐事件提交位置；无新事件时继续处置迟到文件。完成查询在读取持久位置前后复查实际文件，迟到/异常旧文件不能复用旧完成。回答和行动宿主接入相同确认端口；行动报告使用真实绑定。已有真实回答迟到文件、确认前/后进程退出、异常文件、报告读取竞争和行动宿主证据；最终复核与完整验证尚待完成。

初次 Spec 审查未发现明确额外业务范围扩张；其余已检查的删除、原使用、派生清理、原效果恢复、独立权威证明及四维报告路径未发现可定位错误。18 的真实模型验收保持独立未完成。

初次审查汇总：Standards 硬违规 0、P3 判断性建议 2；Spec P2 部分需求 1。修复后的结论需由补充审查及完整验证确认，不能用本记录提前关闭 19。


## Standards 补充审查

复核范围 `git diff 3b576b0...c1ed4a0`：原Duplicated Code建议已解决，两个入口共用事务内retireSnapshot，容量、来源水位与提交仍由调用方负责。新增Checkpoints消费者及双宿主接入未发现明确规范硬违规或值得新增报告的明显smell；维护、只读检查和持久确认职责明确，复用SourceConsumer。原Refused Bequest保留为P3判断性接口建议，未升级为权限缺陷或验收阻断。未修改文件或运行测试。

Standards：硬违规0；原建议解决1，保留P3建议1。

## Spec 补充审查

原P2已解决，未发现此次修复引入的新Spec缺陷。Checkpoints通过原SourceConsumer登记配置并逐事件确认，维护或检查失败不会推进位置。双宿主绑定固定路径/任务/配置，行动报告使用真实消费者；无新事件仍处理迟到文件，报告在读取位置前后检查实际文件。已检查真实确认前后退出、异常文件、读取竞争和两个宿主再次迟到的测试与通过记录。retireSnapshot提取保持原事务清理语义。

复核范围 `3b576b0...c1ed4a0`，未重复全套；最终完成须依据整票验证。Spec：未解决缺陷0。

## 恢复报告回归增量审查

完整验收暴露四个撤权恢复用例的历史计数为零。修复将最终历史核验移出新执行条件，使用允许持久退休身份的维护核验；新执行的严格有效检查保持。Standards 未发现本次增量的硬违规或新增明显 smell，原 P3 接口建议保留。Spec 未发现新增缺陷：缺失历史仍拒绝，退休必须持久证明，Core 未绑定间隙需单独证明，报告核验不授予新执行权。两项均为代码审查，实际测试结论另行记录。

## 整包预算增量审查

9f2cccf：Standards未发现硬违规或新增明显smell；Spec确认45分钟整包预算没有减少用例、race、单例期限或断言。但Spec补查指出原失败日志还有旧版本执行升级的独立失败，不能全部归因于超时。该失败已单独复现并进入诊断，整票未通过。

## 旧内容显式迁移审查

Standards：本次增量未发现规范硬违规或新增明显smell；迁移职责集中于显式本地入口，复用现有授权及使用登记。此前P3查询接口拆分建议保持。

Spec：显式迁移符合10/11升级恢复及19隐私边界，未发现新缺陷。外部固定清单核对精确原记录、PUT身份、主体，宿主核对原Started调用及输入关联；当前权限、来源、水位、留存与正文完整性均复验，同事务登记当前使用并绑定原对象，不伪造历史授权。普通读取及已退休对象仍拒绝。原升级测试保留COMPLETED、成功结果、原许可及单次效果断言。结论限定于受信升级入口，实际部署不能从待升级数据库现场生成清单充当独立证明。

审查后补充迁移前后真实进程退出、重开重放测试，均通过（19-legacy-migration-process.log，race3.140s）；整票完整验收仍须重跑。
