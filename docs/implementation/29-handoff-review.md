# 29：实施审查

范围：相对 `5e912aa29640b78ce7c4fc30ef55cf451eccf804` 的本票工作区改动和新增文件。两个独立代理按 code-review 分别检查 Standards 和 Spec；依据票据 Agent Brief、协作移交与恢复架构、AGENTS.md、tracker/domain 约定及十二项代码异味基线。运行结果单独见 [验证报告](evidence/29-handoff-report.json)。

## Standards

初审 P3 判断性命名建议：`delegationOperation` 已查询移交操作，名称不能表达实际范围。统一改为 `collaborationOperation`，复查关闭。父路由、安全取消、接纳窗口及 WS 增量未发现文档硬违反或值得引入额外抽象的代码异味。四个 typed 子调用包装与既有接口一致，无须额外通用层。

Standards：1 项建议关闭，0 项未关闭。

## Spec

已发现并修复六项边界缺口：

- P1：新父节点不能以实际认证身份访问既有子任务。加入双方签名激活路由、当前策略和子端持久 ParentRoute；真实不同 mTLS presenter 验证原子任务、原派生额度和取消结果。
- P2：安全取消后同一目标准备记录阻断新的移交。加入源端签名撤销证明和 DiscardPrepared；旧撤销不能覆盖 ACTIVE。
- P2：原接纳窗口只有快照、缺少可用核验路径。加入原发行权威验证和目标任务作用域绑定；验证开放窗口受理、关闭窗口拒绝。
- P1：Accept 既有请求重放绕过 ParentRoute。与查询共享精确路由门禁，在返回前执行当前路由策略；迁移前重放成功，迁移后旧 peer 被拒绝。
- P2：新 Handoff.Begin 漏掉导入操作的任务作用域检查。补齐共享校验；跨任务测试原先返回成功，修复后拒绝。

收尾自查还修复历史报告被按操作覆盖后绕过旧版本披露策略的问题：Facts、ReceiveFacts、DrainAborted 与 Status 检查每条保留修订，Status 同时检查持久检查点并在拒绝时返回空结果。旧版本拒绝、后续版本允许的回归用例覆盖此路径。

增量审查确认相关修复关闭，未发现新增实质问题。后续又验证连续移交、暂停保持、原请求重放、当前策略拒绝、无关任务隔离和资源边界。运行结果与限制按实记录，不将状态夹具等同真实执行或完整系统验收。

Spec：6 项问题关闭，0 项未关闭；恢复依赖受信宿主的实际隔离和内容可恢复性证明。
