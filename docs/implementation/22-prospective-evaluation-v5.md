# 22 票真实模型评价 v5

执行前登记，沿用 v4 的材料、八例顺序、预算和独立语义标准。v4 运行 6/8，两个获取失败样本因搜索摘要被写入无正文引用的 claim 被原校验拒绝，原结果完整保留。本轮仅修正模型输出契约：每个 claim 至少一条真实正文引用，无可用引用时 claims 为空，不在答案重复未经证实的摘要数值。能力版本 citations-v4，批次 search-small-prospective-v5。

最多八次外部请求，每例一次无自动重试，输出上限 512 token；其他限制沿用 v4 execution-lock。新预留 8 元，前三轮预留共 24 元继续保留，均非实际账单；用户累计模型费用低于 500 元授权不变。本轮仍为已知材料回归，失败不移除，不声称泛化质量。实际 HTTP schema 测试先红后绿，Ark/Brain race 与相关 vet 通过，Standards 静态审查无发现。全部生成后另做独立语义评价。
