# 23：双轴代码审查

范围：本票实现、协议注释与生成结果、验证入口、测试及实施指南；不包括用户正在修改的 `.agents/skills` 文件。两轴由独立并行审查者完成，随后分别复核修复。

## Standards

最终无可行动发现。节点登记及入口身份复用已有原子授权存储；TLS 留在 adapter；证书准备在事务外，提交前重新核验。既有操作分区拒绝节点管理操作的身份复用。新增内容读取验证复用既有受控内容实现，宿主限制使用现有网络库与标准 HTTP 配置。

覆盖：AGENTS.md、领域文档、工作包共同约束及 code-review 的 smell baseline；审查者进行了静态审查，没有自行运行测试。未发现需要增加抽象或重构的规范问题。

## Spec

初审 3 项，均已修复并由原审查者复核关闭：

1. **P1：TLS 验证只返回预留操作，未执行业务读取。** 修复为调用既有 `artifacts.Service` READ，核对实际 `hello` 内容，并在披露前重新核验 Grant。
2. **P1：未持久保存业务操作的原始入口身份。** 增加 `NodeJournal.Presentations`，只在已预留的原操作和当前资格有效时保存入口证书及关系；验证从原 SQLite 文件的独立句柄重建授权对象，读取原记录并重新核验。证书退役后该恢复资格被拒绝。
3. **P2：验证宿主未限制连接与握手。** 增加连接上限、请求头/读写/空闲期限及请求头大小限制；实际无握手输入连接会被服务端关闭。

最终无新增可行动发现。单次子授权回归验证了重签后的原操作身份、祖先分配和范围保持。

覆盖说明：业务身份恢复证据是独立 SQLite 句柄与重建授权对象；实际进程退出证据单独覆盖登记管理恢复，不混称为完整远程 Worker 恢复。完整 gRPC/WebSocket 仍属 24/25。最终机器报告和票据状态由主执行者对实际输出核对，不属于审查者的代码快照。

Standards：0 项；Spec：3 项已关闭、0 项未解决。已知基线记忆存储回归失败另见验证报告，不隐藏为全量通过。
