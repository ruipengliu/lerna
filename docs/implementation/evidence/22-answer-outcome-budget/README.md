# 答案装配账本读取的实际计费

实际执行 `go run ./cmd/searchcheck -profile frozen-loopback-v1 -case all`，退出 0。原公开虚构材料、本地 HTTP、本地协议模型与 64 条查询额度不变；报告保留实际构建信息和 not_evaluated 标记。

| 用例 | Core 总查询 | 其中 Outcome 查询 |
| --- | --- | --- |
| answerable | 35 | 4 |
| insufficient | 35 | 4 |
| conflicting | 49 | 6 |
| fetch_failed | 40 | 4 |

行动宿主在一次串行任务运行中复用已经取得的不可变终结结果，不缓存未知结果、Content 正文或权限。重开之后不会继承此内存缓存。答案阶段对每个已结算操作读取一次账本并计费，将同一结果用于页面/失败选择和来源继承；每次使用引用仍经当前 Content/来源权限检查。没有通过更改分母或免计实际存储读取保持额度。

这些结果只覆盖参考运行的上述读取。行动宿主其他内部 Content 读取与完整计量审计仍待完成，不能据此宣称 22 票或真实模型效果通过。
