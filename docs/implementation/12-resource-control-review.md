# 12 实施审查

基点 `d59cf762b7b7f3ca7b6a0b24d64fc1738500a575`，固定比较 `git diff d59cf76...HEAD`。初始提交 `9e37227`，修复提交 `eed2658`。按 code-review 技能分别安排 Standards、Spec 独立并行审查，再由原审查者独立复核修复。

需求来源为 12 票已确认 Agent Brief、工作包约束和执行/资源控制架构。规范来源为 AGENTS.md、docs/agents、CONTEXT.md，以及技能中的判断性代码气味基线。

## Standards

初始 1 项 P3 判断性建议：重开存储和事务结束分别复制相同的调用索引投影逻辑。现统一为 `resourceJournal.indexInvocation`，两个入口保留各自身份冲突校验。独立复核关闭；无硬性文档规范违规。

新增的同快照目标读取、分区解码复用以及有限可取消 CAS 退避均经复核，未发现追加问题。最终剩余 0 项。

## Spec

初始 1 项 P1：目标端分两次读取“作业不存在”和“更高隔离代次”，迟到请求可能在两次读取之间真正完成。新代次只能保证后续隔离，不能证明之前没有完成效果；旧实现可能误报 NOT_OCCURRED。

现使用同一只读事务读取作业和控制代次，关联句柄不匹配明确拒绝。确定性回归先固定无作业的旧快照，再由另一连接提交 Start、Complete、TAKEOVER，确认旧快照仍为 UNKNOWN，新快照保留 CONFIRMED。独立复核关闭；最终剩余 0 项。

首轮完整验证另发现旧双协调器并发回归耗尽 CAS 重试。针对性重复检查复现后，保留 8 次上限并加入 1–64 毫秒可取消退避，复用单事务已解码分区以减少重复解析。旧回归连续 30 次通过，新增有限次数、取消和无部分提交测试通过；两个审查者均复核此增量。首轮失败未计为通过，最终结果见实施指南及归档完整日志。

Standards 初始 1 项、Spec 初始 1 项，最终均为 0 项；各轴均无未关闭问题。
