# 25 号票代码审查

范围：本票工作区差异与新增文件，基线 `4466a68a06b2ef34a864072e0cdc8320df260aaf`。两名独立审查者读取同一份初始快照及修复后的增量快照，分别执行 Standards / Spec 审查。主执行者负责测试和最终报告核对。

## Standards

初审未发现需要修正的明确规范违例或基线 smell。检查 AGENTS.md、issue-tracker、triage-labels、domain 和 CONTEXT；双向连接共用 Peer、Binding 复用目录和执行服务、TaskRef 提取解除真实依赖环均有当前需求依据。生成代码的重复结构未作为手写 smell。

增量复审未发现新增问题：控制消息分块的同流序号修复集中在共同发送路径，新增测试复用真实连接和授权夹具。未重复格式、构建和静态检查。

Standards：0 项可执行发现，范围内无未关闭问题。

## Spec

初审发现 2 项 P2，均已关闭：

1. 控制消息自身分块时允许同流消息插队，后一序号可能先完成重组导致连接被拒绝。修复为控制消息分块完成后再发下一控制消息；其他流仍在分块间让控制通过。真实链路测试修复前发生异常断连，修复后竞态测试通过。
2. 部分明确验收缺少直接证据。新增进程内/网络结果和授权错误等价、两个在途请求倒序响应、实际超时后的迟到响应、成功关闭可选能力、并发重组项数上限、订阅关闭后的交付验证，最终专项日志均通过。

增量复审确认两项关闭、未发现新问题。审查者未重跑测试；主执行者运行并保留原始结果。

Spec：原 2 项已关闭，新增 0 项。较宽目录回归失败单独记入验证报告，不能由 WebSocket 专项通过替代；最终报告由主执行者核对，未包含在两位审查者的代码快照中。

最终协议兼容性增量另经 Spec 复审：TaskRef 的 public import 保留旧消费者导入方式，实际独立 proto 编译探针在修复前失败、修复后通过，生成 public dependency 正确；无新增未关闭发现。
