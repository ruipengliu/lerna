# 27：实施审查

范围：相对 f507da1 的本票工作区改动及新增文件。依据本票 Agent Brief、授权生命周期、端云恢复规范、共享实施约束，以及 AGENTS.md、tracker/domain 约定和 code-review 的十二项代码异味基线。两个独立代理分别检查标准与契约，修复后进行增量复查。

## Standards

初审无文档硬性违反，一个 P3 判断性 Speculative Generality：未使用的 OfflineView.Confirm 与实际应用确认路径重复。已删除该入口，保留接收端 Confirm 经认证读取请求报告应用水位的路径。源端 Read/Progress 共用刷新逻辑。增量复查未发现新增标准问题。

## Spec

初审发现三项问题，均先增加失败检查，再修复：

- P1：Go 时间值带单调分量，墙钟相减可能掩盖实际墙钟偏差。系统墙钟样本和偏差比较均剥离单调分量，增加真实时间样本检查；异常分支另用可控时间源验证。
- P2：源端进展仅看已生成版本，离线撤销后可能仍显示没有待送达变更。进展查询现在重新计算当前权威有效判定。
- P2：分页过期仅清 After，保留旧页版本和偏移，首次快照也不能重取。现在完整重置快照查询，最多重取一次并保留总 I/O/页数上限。

增量复查确认三项关闭，没有新增可操作缺陷；精确操作的有效授权视图和受信宿主恢复门禁的责任符合本票范围。审查提出的运行证据缺口进一步补齐：接收子进程通过真实 mTLS 获取页面、持久接收后以 77 退出；下一进程应用副本但拒绝离线启动，独立目标仍为零变更。重新在线核验后原操作完成。

未执行真实设备休眠或修改宿主系统时钟的实验，不以可控时钟故障注入替代这些组合。最终运行状态与原始证据见 [验证报告](evidence/27-offline-report.json)。

Standards：1 项建议关闭、0 项未关闭；Spec：3 项问题关闭、0 项未关闭。审查结论不等同全部平台或完整系统验收。
