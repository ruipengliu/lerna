# 22 票公网任务实际验收记录

**当前结果：用户改为直接使用豆包Summary后，总结路径v4已通过本次有限公网验收：搜索1次、原网页0次、正式答案、事实2/2及引用支持4/4。** 见[总结模式说明](22-search-summary-mode.md)与[独立报告](22-public-summary-review.md)。以下旧版本失败按当时要求保留；22票的全仓回归问题仍未关闭。

2026-09-13。v1在创建任务前失败，零模型请求；宿主创建私有目录的缺陷已修复（ef8c23a）。v2在执行前登记，使用真实豆包搜索与真实豆包答案模型，未自动重试。配置、命令、代码绑定及原始输出见 [证据目录](evidence/22-public-v2-ef8c23a/execution-lock.json)。私有任务目录及宿主令牌没有归档。

**公网完整问答验收未通过。** 正式治理链取得真实搜索候选，但四个页面候选中两个不在预先授权范围内而拒绝，两个 httpbin 页面请求超时，未取得任何正文。模型产生获取失败说明，却达到512输出token上限被截断，原校验拒绝发布，没有正式答案。不能将连通性预检、搜索成功或未发布的模型文本写成问答通过。

按模型输入中原始取得记录的 Mode/Requests 计数，搜索1次，页面HTTP尝试2次，另两项拒绝不代表外部访问；ResearchUsage 的 SearchRequests/PageRequests 是参考服务器本地计数，公网配置中为0，不可将其解释为公网零访问；原网络扣额和查询用量保留在public.json，超时和拒绝未退款重置。模型实际输入1819、输出512 token，一次调用。DNS预检已有 httpbin.org 解析失败，www.httpbin.org 能解析；配置前已明示该限制。单任务搜索超时3000ms、页面1000ms，未通过放开任意目标或增加重试掩盖失败。

待完成：使用可实际获取的公开正文完成完整链路；在保持有界预算与原失败记录的条件下，为多来源失败报告选择足够输出空间或更精简的合法表达；独立核对最终正式答案引用。当前无需用户再次提供模型/搜索密钥或重复批准500元模型预算，但公网正文可达性和模型输出完整性仍未满足本次验收。

当前归档的v2至v6五轮参考评价及本次公网调用合计41次真实答案模型请求，输入52816、输出12221 token。各轮管理预留不是账单；没有读取平台账户实际账单，也不据此认证精确人民币消费。继续遵守开发阶段累计模型费用低于500元的授权。

## 用户取消固定512上限后的v3

代码0b3eb04将输出额度配置化，公网CLI默认4096；执行前登记14e3dcc，原4096配置经任务预留与模型请求使用，运行记录见 [v3执行锁](evidence/22-public-v3-0b3eb04/execution-lock.json)。测试覆盖真实HTTP请求4096额度及600输出token的计量，以及正式任务恢复后仍使用原4096额度并结算600输出token。旧任务零值512及旧参考条件保留以支持恢复和复现。答案另受独立8192字节上界约束。

v3真实模型输入917、输出207token，Finish=stop，发布fetch_failed。但本轮在搜索阶段产生denied，仅一次搜索尝试、零页面请求，没有复现v2的四来源输入，因此不能声称已证明多来源截断消除。独立复核还发现模型将失败误归因为httpbin页面请求被拒；实际没有页面请求。这是尚存的语义问题，结构发布成功不能算问答验收通过。

v2、v3两个公网真实模型调用加五轮参考评价共42次，输入53733、输出12428token。管理预留仍不等于平台账单。下一步应明确失败上下文中的搜索/页面阶段，取得可达的公开正文，并完成当前版本全仓验证；22票继续in-progress。
