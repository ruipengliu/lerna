# 11 实施审查

用户确认基点 `fa498ce057f217be06c914793225216d8448b707`。固定比较命令 `git diff fa498ce...HEAD`；初始实现 `e38ece7`，修复 `641a9e1`、`054deb7`。按 code-review 技能独立并行审查 Standards 与 Spec，并对两次增量复核。

规格为 11 票已确认 Agent Brief、工作包共享约束及执行/恢复架构分册。标准源为 AGENTS.md、docs/agents、CONTEXT.md、工作包规范及技能的判断性 code smell baseline。

## Standards

初审 1 项 P2：Service 保存了调用者可修改的 AsyncPolicy 指针，与固定版本及摘要约束不符。构造时现已复制策略，构造后修改调用者配置不再改变正在使用的能力；回归测试先失败后通过，独立复核关闭。

最终遗留：0 项。

## Spec

初审 3 项 P2：

- 终局后迟到进度产生一致的新报告修订，但 Core 拒绝该修订，导致 Outbox 无法排空。现在允许一致终局的后续修订保存，不重复推进已终态任务。
- 恢复入口反复取同一排序前缀，未启动或无法继续核验的记录可能挡住后续工作。现在持久轮转游标，有界轮次能越过空闲接纳；重启沿用游标。
- 首次 Start 前 Task CANCEL 已推进版本，PreStart 处置仍要求完整旧版本匹配，无法释放原工作。现在核对原任务拥有者、工作和代次，允许控制版本变化，且不能覆盖已绑定另一执行操作的工作。

三项均有回归，独立复核关闭。额外验收覆盖缺口已补齐：过期原键查询保持 UNKNOWN；目标持久 Fence 提供覆盖充分且拒绝迟到 Start 的否定证据。实际 fa498ce 旧编码升级测试验证原启动/许可、目标及后续核验。

最终遗留：0 项。

主任务另行补测并修复完成时间与取消截止、取消和原许可身份隔离、进度入口并发限制。初次完整验证在 go vet 检查到三个夹具字面量未写字段名，修复后重新完整验证；准确结果见实施指南。未用开发检查代替最终全量证据。

Standards 初始 1 项、Spec 初始 3 项，最终均为 0 项；不存在未关闭的最高严重问题。
