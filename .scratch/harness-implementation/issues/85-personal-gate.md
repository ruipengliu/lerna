# 85: 验收个性化效果与隐私出口

**What to build:** 完整运行 400 对任务，各适用/边界组达≥95%；检查模型、日志、云存储和派生输出的端云限制。

**Blocked by:** 47：构建个性化成对对照基准；48：恢复、取消和重新判读评测；63：替换记忆检索与组织实现；39：在端侧完成私有推理与交付；20：从授权来源自动提取端侧记忆

**Status:** ready-for-agent

**能力映射:** C2、C7、C8；A3、A4

**规范依据:** 专项验收、持久化替换与实施交接；端云记忆驻留与联合检索。共享实施及报告约束见本工作包规格说明。

- [ ] 同候选运行全部 400 对任务，适用回答/行动各 100，边界四类各 50；适用子组和各边界类别正确处置分别≥95%。
- [ ] 两臂独立重置业务及可变记忆，逐臂判定允许结果，每对只计一次；适用个性化必须实际改变可观察输出/动作，不能只看 Memory 调用。
- [ ] 每类≥一半端侧来源，严格留端≥40 对、有限派生披露≥40 对分别验证；至少包含授权提取资料的来源/修订继承。
- [ ] 检查云数据库/索引、模型请求、消息、日志/评测及派生出口，全部隐私、删除和用途必需断言通过；无获准本地计算时正确等待。
- [ ] 每臂模型≤8/token≤131072、新操作≤8/辅助查询≤64、300 秒，纠错及补发≤2，按共同并发；保持任务对和来源族的相关信息。
- [ ] 输出同候选成对证据、逐类分母和判断，未达标明确失败/不完整，不用 API 或 GUI 指标替代个性化效果。
- [ ] 提供可重复验证入口，记录实现/依赖、配置、环境、执行方法、实际结果与限制；未运行或失败不写成通过。

