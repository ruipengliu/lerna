# 14: 让大脑选择 API 并完成任务

> *This was generated by AI during triage.*

**What to build:** 从完整目录形成有限行动批次，Core 原子准入并按效果继续决策；保留首次错误，有限纠错不重复未知动作。

**Blocked by:** 08：在预算内生成并发布答案；11：恢复异步与未知效果调用；13：在千级目录中检索准确版本

**Status:** ready-for-agent

**Implementation:** completed（2026-09-11；本地契约及真实多动作小回归，保留 Ark 单步包络限制）

**能力映射:** C1、C4、C5、C7；A1

**规范依据:** 大脑循环与多 Agent 协作；决策准入、交互与协作恢复；工具执行与多模拟设备。共享实施及报告约束见本工作包规格说明。

- [x] 真实模型从完整目录检索、加载候选并提出有限行动批次，经 Core 校验后调用实际有状态 API；动作结果驱动下一步直至目标或明确等待。
- [x] 批次局部依赖映射为稳定操作，循环依赖、超大批次、非法类型或超预算整体不准入；批次外部效果不宣称原子成功。
- [x] 生成、检索模型使用、辅助查询、执行和纠错均计入预留预算；隐藏修复不能改写首次提案，未形成提案或候选遗漏可计首次失败。
- [x] 准入同时比较任务版本、owner/epoch、Worker 资格及控制意图；提交不明先核对原变更，已准入动作恢复不重新让模型生成替代身份。
- [x] 通过单步、多步、必要参数缺失、只读错误纠正、错误写入和无进展夹具；错写后再写对不能当成功，未知效果先核对而非重试。
- [x] 给出小型真实模型回归记录及范围；本票运行成功不宣称千级完整质量已经达到 90%/95%。
- [x] 提供可重复验证入口，记录实现/依赖、配置、环境、执行方法、实际结果与限制；未运行或失败不写成通过。

## Agent Brief

用户已接受以下实施边界。分类：enhancement。以同一个 Task 的模型提案、批次准入、实际 API 效果和后续决策构成闭环，不能为每个动作另建 Task，也不能将单次 API 成功等同于任务完成。

### 接口与行为

- 在 Model、Brain、Catalog、Core、Execution 和持久化恢复接口验证。保留答案生成契约；行动策略与供应商适配隔离，模型输出不具有执行权。
- Core 原子验证提案类型、局部依赖图、动作上限和剩余预算，保存稳定操作映射。模型请求、目录与状态辅助查询在离开进程前预留；未知请求保留完整额度。
- 首次提案及错误不可被格式修复或纠错覆盖；正常多步推进不消耗纠错轮次。错误写入不可通过后续补偿转为成功。未知效果先查询原操作，禁止换身份重做。
- 准入和实际启动均验证当前任务版本、owner/epoch、Worker、授权、控制与截止时间。重启优先恢复已存提案和已准入动作；提交结果不明查询原提交，不能重新调用模型替代。
- 目录及输入数据依据真实模型处理位置分别授权，不能以执行授权替代数据来源策略。

### 固定边界与验收

- 单步：最多 4 次模型请求、65536 tokens、3 个新操作、32 次辅助查询、120 秒；多步：12 次、262144 tokens、12 个新操作、128 次辅助查询、600 秒。最多纠错 2 轮。批次最多 8 个动作，依赖失败不执行其下游。
- 持久化记录必须有尺寸与数量上限。新增状态应兼容已有无行动状态的 Task；01–13 契约和恢复验收保持有效。
- 单步、多步、缺参数、只读错误纠正、错误写入、无进展、原提交核对及未知动作恢复为本地必验行为。真实模型小回归另行记录授权调用上限、版本、请求/用量和独立效果判定。
- 不扩大为完整千级质量评测。真实模型未运行、失败或不符合硬预算时如实标记，不能用脚本结果代替或提高门槛中的额度。

### 实施前基点

`49b27dc5c066ead0e3f6dc0b3ccf2b5960e7dec8`。实施时曾待真实模型预算授权，后续用户已确认 12 次上限，实际记录见下。

## Implementation evidence

实现 `5e15924`，审查修复 `167051f`。同 Task 行动循环、受控提案、整批准入、预算、实际效果与输入等待/回复已接通；过期租约、稳定身份和未落盘 Invocation 的恢复已验证。完整 make verify 通过，14 票本地 profile 12/12，01–13 回归 1395/1395，合计 1407 项。race、编译、生成一致性和 vet 通过，独立 Standards/Spec 复审无剩余代码发现。

用户确认本轮最多 12 次真实请求、每次最多 1024 输出 token。实际 2 次、5228 token。初次任务因宿主未交付 InputRefs 正文进入错误等待；保留原失败，修复 ddc6903 后新任务通过：一个真实提案、两个依赖动作，authorized 状态、账本 997。修复后完整 make verify 再次通过 1407 项，双轴增量复审无剩余代码发现。

本票按已确认范围完成：单步等行为为本地必验，真实小回归另报。Ark 单步 65536 token 包络仍不支持；一次真实模型批次成功不证明真实模型多轮重新规划或完整 90%/95% 质量通过。

实现、配置、限制及具名证据见 [14 实施指南](../../../docs/implementation/14-api-brain.md) 和 [双轴审查](../../../docs/implementation/14-api-brain-review.md)。
