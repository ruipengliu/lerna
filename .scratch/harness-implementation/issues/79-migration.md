# 79: 双向切换存储并恢复迁移故障

**What to build:** SQLite↔bbolt 经共同维护屏障和唯一活动配置切换；逐窗口故障、旧进程、迟到消息和旧备份均验证。

**Blocked by:** 78：停写并校验跨后端逻辑导出导入

**Status:** ready-for-agent

**能力映射:** C5、C6、C7；A2

**规范依据:** 专项验收、持久化替换与实施交接；恢复状态与故障检查表。共享实施及报告约束见本工作包规格说明。

- [ ] 完整受控节点执行 SQLite→bbolt 与 bbolt→SQLite 停写迁移，通过已校验逻辑包切换后恢复原任务、操作、授权消费、记忆和阶段工作。
- [ ] 共同维护屏障、唯一活动配置和启动代次防止两个后端同时新写；确认旧进程停止，分别持有文件锁不当跨后端排他证明。
- [ ] 迁移记录和切换配置具备自身耐久恢复，切换不明先查询原记录，不猜当前后端；目标先核对权限/时间、未交接工作和外部未知效果再开放。
- [ ] 目标尚无新写时可按规则回到源库；目标已经新写后须重新停写并反向迁移，不能直接启动陈旧源库。
- [ ] 逐步注入进程中断、同步错误、缺引用、部分目标、仍活旧 Worker、迟到回执和旧备份，验证无双写、旧操作复活或授权/删除水位回退。
- [ ] 报告实际支持平台、文件系统与迁移方向，明确停机和失败处置；不声称热双写、多主切换或自动跨云接管。
- [ ] 提供可重复验证入口，记录实现/依赖、配置、环境、执行方法、实际结果与限制；未运行或失败不写成通过。

