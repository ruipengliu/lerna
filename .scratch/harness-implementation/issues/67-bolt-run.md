# 67: 在 bbolt 上运行并恢复任务

**What to build:** 实现 RunStore/工作原子边界，复用 SQLite 任务套件；验证同步错误、提交未知、有界排队和真实重启。

**Blocked by:** 04：运行并恢复有限任务

**Status:** ready-for-agent

**能力映射:** C5、C6；A2

**规范依据:** Go 内核与本地持久化；专项验收、持久化替换与实施交接。共享实施及报告约束见本工作包规格说明。

- [ ] 采用固定 bbolt v1.5.0/已确认提交实现 RunStore 与工作持久化，实际提交、运行并在关闭进程后恢复同一任务，SQLite 保持默认。
- [ ] 每次原子变更在同一文件事务内包含状态、资格/版本比较、工作、回执和 Outbox；接口不暴露 bucket，不能跨文件拆 Commit。
- [ ] 参考使用 Update 且保留同步，事务闭包不做外部调用；提交前有界排队，取消排队与已经开始提交分别解释。
- [ ] I/O 或同步错误后停止相关新变更，经受控恢复核对耐久回执，不凭故障实例缓存报告成功；超时不能断言未提交。
- [ ] 基础版本/重复/并发共同套件与 SQLite 复用，重启和故障单独记录；一个文件单读写进程，跨进程访问走受控 Adapter。
- [ ] 首验 Linux amd64/本地文件系统，报告工具链、内核、挂载与同步配置；长读、磁盘满、文件增长及队列均有界，MaxSize 不当 RSS 保证。
- [ ] 提供可重复验证入口，记录实现/依赖、配置、环境、执行方法、实际结果与限制；未运行或失败不写成通过。

