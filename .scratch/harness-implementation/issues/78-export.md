# 78: 停写并校验跨后端逻辑导出导入

**What to build:** 所有相关模块到一致停写切点，导出恢复事实及获准引用，导入未开放目标并验证摘要、引用、水位与索引。

**Blocked by:** 68：在 bbolt 上执行与接管资源；69：在 bbolt 上管理身份与授权；70：在 bbolt 上使用与同步记忆；71：在 bbolt 上管理扩展激活；77：在 bbolt 上评测并推进改进

**Status:** ready-for-agent

**能力映射:** C5、C6、C7；A2

**规范依据:** 专项验收、持久化替换与实施交接；恢复状态与故障检查表。共享实施及报告约束见本工作包规格说明。

- [ ] 有权管理者以稳定操作发起同节点停写导出，停止相关新任务/动作、入口交接及后台写入，排空/停止 Worker 并确认各模块到持久切点。
- [ ] 版本化逻辑包覆盖所选完整配置的运行/工作、执行/资源、记忆、授权、扩展、评测/发布事实及 Inbox/Outbox、等待项、回执、版本资格和删除/接纳水位，不能仅迁 RunStore。
- [ ] 外部未知操作保持原身份/查询信息，已接纳未应用输入可恢复；停机对可恢复输入拒绝或待补送，不保证不可重放的外部通知无损。
- [ ] 数据、派生资产和引用逐项遵守驻留及保留，凭证密文/密钥引用单独受控，主密钥不进入通用包，不能为导出复活已删正文。
- [ ] 导入未开放目标，重建索引并核验摘要、跨模块引用、格式与恢复计划；部分包、超限快照、缺引用或不一致切点不能成为可切换目标。
- [ ] 导出/备份产物发布完成文件及适用目录同步，CopyFile 返回不视为耐久；分别验证 SQLite→bbolt 和反向导出导入，中断后可核对清理而不改源权威。
- [ ] 提供可重复验证入口，记录实现/依赖、配置、环境、执行方法、实际结果与限制；未运行或失败不写成通过。

