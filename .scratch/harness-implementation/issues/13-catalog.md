> *This was generated by AI during triage.*

# 13: 在千级目录中检索准确版本

**What to build:** 在超过 1000 项独立 API 中按权限、位置和预算检索，按需取 Schema；报告索引滞后、部分覆盖及版本失效。

**Blocked by:** 10：执行并确认同步 API 动作

**Status:** ready-for-agent

**Implementation:** completed（2026-09-11）

**Category:** enhancement

**能力映射:** C4、C6、C7；A2、A4

**规范依据:** 工具执行与多模拟设备；执行接口、资源控制与故障验证。共享实施及报告约束见本工作包规格说明。

- [x] 同一可发现目录提供 K≥1001 个业务语义不同的可调用模拟 API，冻结声明及独立资源状态；别名、版本和部署实例不能重复计数。
- [x] List/Search/Describe 分别完成有界枚举、按目标检索与精确 Schema 加载，返回用途、不适用条件、资源、前置条件、副作用及核对/幂等声明。
- [x] 在候选生成前按权限、用途及实际处理位置过滤，目录内容和查询也受披露限制；不把授权外计数或标题泄露到排序解释。
- [x] SQLite 权威目录与可重建索引分开；目录修订和授权视图绑定游标，索引滞后/不可达/预算耗尽与无匹配分别表达。
- [x] Describe 不可用的精确版本显式失败，准入复查版本，不静默换最新；检索遗漏可记录到首次正确性评测。
- [x] 证明完整目录存在时仍有界检索和加载，不能事先按测试答案裁剪目录；吞吐量与真实第三方接入数量不由注册规模推导。
- [x] 提供可重复验证入口，记录实现/依赖、配置、环境、执行方法、实际结果与限制；未运行或失败不写成通过。


## Comments

> *This was generated by AI during triage.*

### 分诊核验（2026-09-10）

维护者已同意按六项边界细化：千级真实可调用、目录到执行闭环、List/Search/Describe 职责分离、候选生成前权限过滤、权威目录与索引分离，以及完整目录下的有界验收。分类为 enhancement，保持 ready-for-agent；不表示本票已实施。

直接前置 10 票已完成，当前代码也已有 11、12 票的异步核对与资源控制。重复实现检查覆盖 execution、sdk、proto、schema、adapters、contractfixture 和 profiles：已有固定能力及实现摘要、Schema 校验、正式执行与受控内容、授权、原操作恢复和共享资源准入；未发现 CapabilityCatalog 或持久目录 List/Search/Describe。现有执行存储按准确描述符分区，并限制最多 32 个分区、总量 4 MiB，不能据此宣称千级目录已能全部执行。未发现相关历史拒绝或 ADR 冲突。

本票需要打通全部目录条目与正式执行链，保留当前执行身份、权限、版本和恢复保证；不能仅增加注册条目或按预知答案切换成小目录。沿用本地 Go/SQLite 和嵌入式文本检索，模型选择循环、完整质量评测及真实网络部署仍由后续票承担。维护者已确认上述范围，无待补充的外部凭证或费用条件。

本轮实际执行 `go test ./profiles/executioncheck ./profiles/asynccheck ./profiles/takeover ./authorization ./schema ./sdk -count=1`，六个包全部通过，验证现有执行、资源控制、授权、Schema 和 SDK 基础。此轮仅细化规格，未运行新的全量验证、真实模型或外部业务 API；已有回归通过不代表尚未实现的千级目录验收通过。

> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement

**Summary:** 建立可持久、可重建索引、按当前权限检索的千级能力目录，将准确能力版本按需加载并衔接正式执行；完整目录下的工作量、覆盖及失败状态可验证。

**Current behavior:**

CapabilityService 与 SDK 已支持固定能力的调用、查询和核对，并提供取消和资源控制。Execution 固定能力语义版本、实现版本、Schema 及摘要，使用真实授权、受控输入、持久启动登记与报告交接。多个准确描述符可以共存，但参考存储当前限制为 32 个描述符分区及有限整份执行状态，不足以承载本票所需全部能力的可执行证明。

尚无持久 CapabilityCatalog、按目标检索、准确声明加载、授权视图游标、索引修订及覆盖报告。模拟后端具备状态和效果核对基础，但没有 K≥1001 的独立业务操作清单及完整调用覆盖。既有执行配置不能直接当作千级目录路由或容量保障。

**Desired behavior:**

目录服务仍属于能力调用领域，提供 List、Search、Describe 的 SDK/本地协议入口；内部依赖消费方定义的 CapabilityCatalog Interface。权威目录、候选索引、准确版本解析和执行适配各自有清晰边界；数据库、搜索引擎对象和供应商 SDK 不进入领域调用参数。查询不要求创建额外 Task 或新的通用身份体系。

参考目录使用 SQLite 持久保存受信声明及修订，索引可删除并重建。本票提供足以冻结、加载及测试声明更新/撤回的受信本地组装或导入能力，不扩展为完整插件安装、市场或分阶段激活系统。声明未经完整校验不能作为可调用版本发布；导入或修订中断不能使目录引用半份 Schema、不存在的实现或错误摘要。

同一运行配置中冻结 K≥1001 项可独立调用且业务语义不同的模拟 API，并保留声明、生成配置及内容摘要。每项具备业务用途、适用与不适用条件、资源、必要参数及前置条件、效果判定和执行保障。共用模拟引擎允许，但每项独立计数须有可审查的业务规则与效果契约；只更换序号、资源实例名、别名、URL、部署实例或重复版本不能充数。不同 API 的独立资源状态或明确共享关系可被核验，不把固定成功回包当作状态效果。

完整目录存在时，每项均能经正式 SDK/执行路由发起合法调用，并由独立于候选检索与路由选择的判定材料核对目标业务结果。实现需要解决目录规模与执行状态、授权、受控内容及任务容量的衔接，所有相关限制保持显式有限。允许按真实职责划分存储或有界惰性装配，但不能按测试答案预先裁剪目录、为规避容量清空历史记录，或给未接入正式执行的条目标注可调用。不把 K 项目录等同 K 个并发任务或 K 家第三方服务。

List 返回稳定、有界的获准摘要页，不随枚举加载全部 Schema。Search 接收目标描述、资源类型、必需能力、用途、允许位置和候选/时间等工作预算；参考实现采用本地关键词、业务分类及别名检索。Describe 只按类型化能力引用、准确语义版本及适用的实现版本/摘要加载完整声明。条目明确输入输出 Schema、资源及用途、实际处理位置、前置条件、副作用、幂等范围及有效条件、取消和原操作核对支持；没有的保证明确不支持，不以默认“安全”代替声明。

能力语义版本、实现版本、Schema 修订和目录修订分别表达。相同准确版本及摘要不能悄悄换成新内容；Describe 的准确版本不可用、摘要不符或实现不可达时显式失败。执行路由接受并核对选定描述符，不按名称转到最新版本，也不把多个实现计为多个 API。

目录可见性不是执行授权。候选生成前按受信主体、授权资源、用途、实际处理位置和数据披露策略限定输入集合，不能先对全目录取 Top-K 再过滤权限。未获授权的条目不能通过标题、数量、匹配解释、索引统计或缓存影响而泄露；读取完整 Schema 和返回结果前仍检查适用的当前权限。调用者指定允许位置只能进一步收窄条件，不能更改真实处理位置或绕过端侧数据限制。查询和声明正文默认在获准本地处理，本票不要求上传端侧目录或调用远端模型。

分页游标为受校验的有界状态，绑定命名空间、主体/授权视图、目录修订和相关查询条件。篡改、换主体、改变过滤条件，以及目录或授权视图失效时拒绝或要求重新枚举，不混接两个视图。缓存同样按授权视图和准确修订隔离；撤权后不得返回旧页、旧候选或旧 Schema。无法确认权限时不凭缓存继续披露。

权威目录修订与索引覆盖分别报告。索引滞后、损坏、重建中或不可达时，可在明确预算内降级为权威扫描，也可返回部分结果及限制；不能把失败或截断冒充“完整且无匹配”。无匹配须说明是在何种获准覆盖范围和预算下得到；授权外范围不进入公开总数。对已获准来源不可达的覆盖语义通过本地可控来源/故障适配验证，不以此宣称完成真实端云联合发现。

新调用准入复查准确版本及声明适用状态，目录更新/撤回与准入之间须有明确且可测试的排序边界；不能在 Describe 成功后无条件启动。已准入调用继续保留原版本、Schema、实现、身份与报告，后续目录更新不将其改写为另一动作。版本固定不豁免当前执行权限、Task/Worker 资格、资源控制或预算检查；原操作重放及未知核对仍沿用既有契约。

所有输入长度、声明及 Schema 大小、页/候选数量、索引及扫描工作量、内存缓存、I/O、重建批次和并发都有显式上限。大目录下不预先把全部 Schema 装入每次查询或决策上下文。取消与预算耗尽及时停止有界工作，保留已获得的覆盖信息；不在目录内部隐式启动模型或无限重排。本票保留可替换检索/排序接口，但不要求实现向量服务或真实模型。

查询结果保留目录及索引修订、实际覆盖、限制和可披露的匹配依据，供后续评测记录候选遗漏与首次错误。本票交付目录及检索契约证据；真实模型选 API、首次正确率 90% 和有限纠错成功率 95% 不由文本检索或模拟执行通过直接推导，也不在本票生成完整每 API 20 例基准。

**Key interfaces:**

- CapabilityService / Capability SDK 的 List、Search、Describe：有界摘要、获准候选、准确声明及明确覆盖/错误语义。
- CapabilityCatalog 与准确版本解析接口：分离目录发现、Schema 按需读取和执行路由；不暴露数据库或索引实现对象。
- 权威目录存储与索引接口：持久修订、受信声明更新、索引重建、降级扫描及中断恢复。
- 授权/数据策略与查询上下文：主体、资源、用途、实际处理位置、披露范围及授权视图变化，缓存与游标不能绕过当前检查。
- Execution / ExecutionStore / Driver：千级准确能力路由、版本准入、有限持久状态和旧操作恢复；保留原许可、身份及资源控制。
- 模拟 API 清单、独立效果判定及具名 profile：完整目录可发现、逐项正式执行、有界查询和故障证据；评测真值不进入检索输入。

**Acceptance criteria:**

- [x] 交付冻结 K≥1001 的独立业务 API 清单及计数核验；每项有可审查的语义、Schema、资源/前提、实际处理位置和效果契约，别名/重复版本/实例不重复计数。
- [x] 同一完整目录保持可发现时，逐项经正式 SDK/执行路由实际调用并核对独立业务结果，输出逐 API 覆盖；不能只有少量特例可调用，不能重建小目录或清空未决历史绕过容量。
- [x] List、Search、Describe 的 SDK/本地协议正反例通过；List/Search 返回有限摘要，Describe 按需加载准确完整 Schema，拒绝非法输入、超大请求和错误命名空间。
- [x] 候选生成前执行权限、用途、实际位置及披露过滤；低权限主体不能从候选、标题、总数、解释、缓存或索引统计取得授权外信息，Describe 也不能绕过。
- [x] 游标绑定目录修订、授权视图和查询条件；篡改、跨主体/命名空间、条件变化、目录更新和撤权后重放均正确拒绝或明确要求重开枚举，稳定视图下无漏项/重复页。
- [x] 本地文本检索在完整目录中处理关键词、业务分类、别名、近义干扰、资源及用途约束和无匹配；检索路径不取得正确 API 标识、预裁候选或独立判定真值。
- [x] 索引缺失、损坏、滞后、重建中、获准来源不可达和查询预算耗尽分别可复现；有界降级或部分结果明确覆盖与限制，无匹配不掩盖故障，授权外数量不泄露。
- [x] 不可用准确版本、错误摘要、同版本异内容及不匹配实现拒绝；不能静默换最新。Describe 后版本变化与新调用准入的竞争有可验证排序，拒绝不产生新业务动作。
- [x] 已准入操作在目录更新/撤回后保留原描述符、Schema、许可和报告；读取/重放/核对仍检查当前权限，不改写旧操作，不重发未知 Start，不绕过 Task 或资源控制。
- [x] 查询、排序和缓存遇到授权变化时不会继续披露旧视图；不可确认授权、非法用途和实际处理位置不符均失败关闭，查询及描述正文不产生未授权外发。
- [x] 千级目录与正式执行的容量衔接有实际证据；存储、授权和内容的限制均明确有限，超限拒绝无部分接纳，跨能力不串写目标或复用原操作身份。
- [x] 提供查询/页/候选/Schema 加载及扫描工作量证据，完整目录下仍按配置上限执行；取消、I/O 失败、慢索引及预算耗尽不形成无界扫描、全部 Schema 预载或隐式模型调用。
- [x] SQLite 重启及真实子进程中断覆盖权威目录修订、索引更新/重建和必要的执行准入边界；重开后准确版本可核对，索引可重建，无半份声明、伪完整覆盖或重复业务效果。
- [x] 若修改执行存储格式或容量组织，使用实际旧编码验证升级，保留同步/异步、未知占用、取消、资源控制及 Outbox；01–12 适用回归继续通过，不用新编码器构造“旧数据”替代。
- [x] 交付具名可重复 profile、可运行本地入口及说明，记录冻结目录、配置、实现/依赖/环境、逐项结果、覆盖和限制；注册规模、检索契约、实际调用与模型质量分别报告。

**Out of scope:**

- 真实模型选择 API、规划和多轮执行循环，由 14 票及后续大脑票承担；不在目录内建立模型调用循环。
- 每 API 20 例及独立保留材料、完整宏平均质量判定，由 43 及 81–82 等评测票承担；既定 90%/95% 门槛不改变。
- 实际 gRPC/WebSocket 部署、远端目录同步与端云联合发现；本票仅提供协议语义和本地可控来源故障验证。
- GUI 驱动、真实设备、第三方 API 接入或付费外部服务；目录规模不证明并发吞吐或供应商数量。
- 完整插件安装/激活/回滚、在线资源范围重映射、自动权威迁移及第二存储后端；仅实现本票必要的受信声明加载与版本变化。
- 自动重试未知副作用、清空未决历史、放宽权限或以候选排名作为执行许可。


## 实施完成（2026-09-11）

提交 `cc70111` 实现千级目录，`bcaf9be` 落实独立数据策略与 Schema 读前授权的审查修复。完整目录固定 168 个业务类型、1008 项独立操作，逐项通过正式 SDK、真实授权/内容/Task/Worker/Execution、共享资源控制、独立 SQLite 目标与报告交接；会话按固定业务命名空间有界装配，全部历史持久保留。

List/Search/Describe、准确版本准入与撤回排序、获准候选预算、数据来源策略、游标修订、索引降级/重建、共享资源未知与接管、SQLite 真实子进程恢复均已验证。当前执行 Format 2 未修改，旧编码与同步/异步/取消/Outbox/资源控制回归继续通过。

最终完整 `make verify` 通过：13 票 1049 项（1008 逐 API、28 边界、8 数据策略、5 子进程恢复），01–12 回归 346 项，共 1395 项。构建为 `bcaf9be`、dirty=false；全包 race、go vet、编译、生成一致性与 SDK 样例通过。首轮发现的既有 RunCancel 通知/槽位释放竞争在定向 30 次中复现，修复后 60 次通过，初轮失败另行归档。

Standards 初始 1 项低优先建议、Spec 初始 1 项 P1 已修复并独立复核关闭，最终各 0 项。模型选择循环、90%/95% 模型质量与完整多例评测、网络部署及真实第三方接入仍由后续票承担。

复现与证据见[实施指南](../../../docs/implementation/13-capability-catalog.md)、[独立审查](../../../docs/implementation/13-capability-catalog-review.md)、[完整契约报告](../../../docs/implementation/evidence/13-capability-catalog-report.json)。Status 保留分诊角色，Implementation 与本节表示实施完成。
