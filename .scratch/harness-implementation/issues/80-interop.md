# 80: 验证独立实现和部署位置互换

**What to build:** 运行共同矩阵，分别记录三系统位置互换、原生绑定、UI 与外部双角色支持；独立对端证据和 M3 成熟度可追溯。

**Blocked by:** 20：从授权来源自动提取端侧记忆；24：跨进程执行并核对能力调用；29：协作移交 Task Owner；35：通过图像完成模拟 GUI 动作；36：按资格从 API 切换到 GUI；53：运行受限 Wasm 计算能力；55：安装并使用自定义 Renderer；58：验证 MCP Streamable HTTP 双角色；59：向 A2A 外部 Agent 委派任务；60：通过 A2A 受理外部任务；62：替换 Brain 策略与 Model Adapter；63：替换记忆检索与组织实现；64：替换独立 API Adapter；65：替换搜索与内容获取 Adapter；79：双向切换存储并恢复迁移故障

**Status:** ready-for-agent

**能力映射:** C1–C8、C9；A1–A4；V1、V2

**规范依据:** 能力里程碑与实施依赖；原生接口与消息目录；专项验收、持久化替换与实施交接。共享实施及报告约束见本工作包规格说明。

- [ ] 以统一测试入口运行三系统及关键 Adapter 共同契约，逐项列出默认与独立替代实现、版本、支持范围和实际证据，不用两个配置或内存替身冒充替换。
- [ ] 同机多进程实际 gRPC/WebSocket 路径与进程内用例比较，交换记忆/大脑/执行部署位置运行任务；独立原生对端或语言无关行为夹具验证协议，不只同 Adapter 自连。
- [ ] MCP stdio/Streamable HTTP 与 A2A HTTP+JSON 分方向记录独立对端、版本和可选特性；UI 多端、多 surface、无任务及自定义 Renderer 分别报告。
- [ ] 完整 SQLite/bbolt 运行配置及双向停写迁移共跑耐久范围，检查全部必需 Store 和受控内容引用；每类实现不支持项不得静默用默认后端代跑。
- [ ] 汇总独立 Brain/Model、Memory 策略、API/设备、搜索/获取、评分器与扩展运行器证据；原有权限、版本和恢复约束在替换后仍成立。
- [ ] 逐项生成 M3 目标/实际矩阵：C1–C8 目标 L3、C9 目标 L2；关键缺口、失败或证据不完整阻止阶段通过，不把规范目标或代码编译算实测支持。
- [ ] 提供可重复验证入口，记录实现/依赖、配置、环境、执行方法、实际结果与限制；未运行或失败不写成通过。

## Comments

2026-09-10 发布检查：补齐已确认范围内的必要前置——20：从授权来源自动提取端侧记忆、35：通过图像完成模拟 GUI 动作、36：按资格从 API 切换到 GUI、53：运行受限 Wasm 计算能力。原拆分中这些票虽进入最终验收链，但未阻塞本票，无法支持本票要求的 M3 完整参考能力及驱动/运行器证据。此修正不新增实施范围，也不把后续完整质量数据集列为 M3 前置。
