# 连续实施进度

完整目标：逐个处理 issues 中所有未完成票据，每票先 triage 状态转换，再 implement；用户已授权过程决策。不得以当前票或本文件替代完整目标。

## 当前权威状态

- 全部 88 票保持在范围内。01–17 已完成各自限定范围；18 实施中；19 已完成 triage/Agent Brief并开始原子删除存储实现；20–88 仍待按依赖和编号推进。历史实施和证据见各票及 docs/implementation，本文件仅保留当前状态。
- 17 最终 make verify 已通过，1474 项必需检查，含可选共 1479 项；两项规格 P2 及 CLI 路由失败已修复，证据已归档。
- [18 票](issues/18-context.md)：ready-for-agent / enhancement / in-progress；已完成两阶段 triage 和 Agent Brief，最终代码审查基点固定 304aeb3。
- 18 已接通 Memory/签名读取/ContextAssembler、Core 事实、动态产物来源治理、实际回答和 API 宿主。原快照、只存引用、缺失元数据、当前权限/修订检查、多决策及真实进程恢复已有定向证据。CLI personalized-context-v1 已扩为 52 项，完整实际 CLI 定向验证通过（437.007s），覆盖新增结算、重组及失败组装历史恢复。
- 失效决策可仅记录固定错误码和用量，无旧正文/提案；未知预留保持。已知且未准入的上下文失效可经显式宿主重组端口进入新 Core 决策，新修订使用新合法读取材料，共享持久纠错计数最多两次。正例改变实际记录；持续更正、撤权、未知调用与重组后的两个进程退出点均覆盖。修复授权准备/组装共用期限后，六项 race 通过（206.148s）；Brain/Core、vet/build/diff 通过，原有四项进程恢复回归通过（56.859s）。
- 失败组装的恢复历史已区分实际绑定快照与 Core 零调用失效缺口；不将读取错误或文档丢失视为缺口。六项进程 race 通过（271.549s），含篡改拒绝及无副作用证明；原正常/重组任务共四项恢复回归通过。
- 18 真实模型的 8 次授权请求已全部使用，共 8430 输入、760 输出 token。API 成对效果通过；详细回答违反 JSON 协议，正式回答对照未通过。原始三轮报告已归档；不能自动重复联网验收或据此宣称 90%/95% 质量达标。
- 18 双轴审查已完成（304aeb3...1f36950）：Standards 0 硬违例/1 可选 P3；Spec 的 missing→创建失效 P2 已由 5485cb7 修复并增量复审通过。真实回答仍未通过，不能标记完成。
- 18 首次完整make verify已成功退出：1526必需/1531总检查，全部阶段通过；代码基准1077297，原dirty构建标记保留。证据18-final及18-full-verify.log。session 30022已结束，不再poll。19后续代码不在这次冻结验证内。

- 回答通用 Worker 已补齐上下文错误到现有等待原因的映射：相关更正/撤权明确等待、保留用量、不发布旧回答，无关更正完成。定向与相关包回归通过，九项实际 Worker/发布边界 race 用例通过（50.932s）。

## 下一步

1. 19 ContextMemory原读/存储条件登记与ContextAssembler原使用验证已接通，回答/行动普通宿主已采用；缺失记忆、引用式恢复、多决策测试通过。模型/发布及首次动作前真实Delete用例定向race通过；已补真实进程退出后删除记录并清理快照、按原Started/UNKNOWN调用核对效果，race通过且不重复扣款。已增加只读消费进度与受信四维删除报告，按实际游标区分pending/applied/not_covered。Memory服务已接入记录/原操作双重当前授权的状态查询，并通过读取中撤权反例。DELETION_STATUS协议/SDK已接通，真实字节链与不一致响应反例race通过。memory-deletion-v1 CLI/profile已接入SDK删除/状态、真实准入比较清理与重开，实际CLI与包race通过，并登记后续make verify阶段。已修复读取后删除仍返回缓存正文的真实竞态，批量校验正文与覆盖依据仍存在；正常历史读取契约与race通过，删除CLI现3项通过。已增加有界清理Worker，真实多轮消费/独立失败/取消等待race通过，删除CLI通过Worker执行批次。回答宿主已自动消费来源/原使用失效并在关闭前等待清理退出；修复自动清理暴露的已退休快照恢复及长恢复续租问题，相关复验通过。下一步行动宿主生命周期、其他运行载荷、本地状态细分及备份隔离。整票仍未完成。
2. 真实回答任务已澄清风格只作用于 JSON answer 字段；用户追加最多两次、每次512输出token的预算问题仍待答复。原8次预算已用完，不自动调用。
3. 19已在d5999cc形成完整Brief并转ready-for-agent；完整Go/race依赖通过后开始实现，受信原子删除存储和Delete/Correct竞争已验证，19已接通当前授权与协议/SDK删除，五包race通过；下一步派生清理、四维完成度、授权侧元数据留存与备份隔离；18真实回答未通过仍单独保留，不由19替代。继续全部88票目标。

## 本轮判断

progress：18首次整票make verify已通过，真实回答仍待预算复验；19原授权上下文绑定及模型/发布/首次动作删除阻断通过定向验证。19尚未完成全票验收，当前没有运行中的测试会话。继续未完成票据，没有满足全局blocked条件。


## 22票收尾更新（2026-09-13）

22票已完成限定范围，最终代码/测试8ff71c6，make verify退出0、28阶段通过，见[最终验收](../../docs/implementation/22-final-acceptance.md)。豆包总结直接作答的有限公网v4独立核验通过；历史质量失败、非盲测限制及旧catalog间歇失败根因未证实均保留。此次没有开始新票，也不将22完成用于替代18或83的独立质量验收。上方早期进度保留为历史。
