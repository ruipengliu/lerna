> *This was generated by AI during triage.*

# 22 分诊预备记录

本记录仅在等待 21 全仓验证期间收集依据，不改变 22 票状态，不代表依赖已完成或已开始实施。正式分诊时重新核对工作树、21 的终态及既往结论。

## 已核对的现状

- 22 原票要求从信息需求经可替换搜索 Adapter 发现来源、取得正文，再由 Brain 综合形成带证据引用的正式答案；必须分别运行可回答、证据不足、来源冲突、获取失败四类。
- 21 已实现授权 HTTP/固定资料获取、受控 Content、原操作预算/恢复、Core/SDK/ActionBrain 链路及 Task Context 成功证据和有限缺口。代码双轴复核已解决三项缺陷；全仓验证仍在运行，公共 HTTPS 两次 DNS resolve 失败。
- Brain.AnswerBrain 的当前答案形状是 text 与 sources。解析可以约束来源引用集合，尚未发现把关键结论绑定到真实片段、核对支持/覆盖、显式表达冲突与不足的完整搜索问答路径。
- Catalog.Search 是能力目录检索，不是网络信息搜索。不能因方法同名把它当作本票已有实现。
- 本次按领域概念检查了 brain/answers、fetch、fetchcontext、fetchoutput、fetchcheck，以及架构中的信息获取、联网问答和质量判定条款。未发现完整重复实现；未发现可读取的既往 .out-of-scope 拒绝记录。

## 正式 Brief 应保留的边界

1. 搜索结果是获准候选，摘要和可打开链接不是已取得的关键证据；搜索不能扩大获取来源、网络范围或个人信息披露权限。
2. 搜索、抓取、模型及必要查询均计入原任务预算；恢复不得重新生成或重新抓取后冒充原结果。
3. 引用需关联实际取得材料及时间，明确片段范围和适用时效。引用存在性不能等同于支持性，模型不能自评通过。
4. 可回答、证据不足、冲突及获取失败分别表达；冲突不能默认按多数或较新来源覆盖，也不能把获取失败伪造成来源不存在。
5. 受控答案和中间上下文继承全部原始与个性化来源限制；撤权、过期、不可达等路径不能绕过当前授权，不能自动写入长期 Memory。
6. 单机仍以 Go、SQLite、本地组件启动；搜索/模型服务按能力配置，不成为常驻基础设施前提。API 优先，GUI 不属于本票实现捷径。
7. 固定材料、实际 HTTP 及公共联网分别记录。引用支持与答案质量需预先登记独立核对依据；协议夹具不可标为真实模型效果，完整 V1 400/120 例及分项门槛保留给 83 的独立验收。
8. 不擅自使用 .env 或扩大付费模型预算；此前 18 的额外模型预算仍未授权。本票后续若需要付费验收，先完成可在现有授权内完成的实现与验证，再处理确实必要的外部条件。

## 可追溯依据

- 原票：issues/22-search.md；共享约束：spec.md。
- 架构：docs/architecture/00-design-constraints.md、07-tools-and-simulated-devices.md、10-evaluation-contracts-and-release-gates.md、11-specialized-validation-and-handoff.md、11-milestones-and-validation.md。
- 当前实现：brain/answer.go、answers/content.go、adapters/fetchcontext/context.go、adapters/fetchoutput/failures.go、profiles/fetchcheck。

预备分类建议为 enhancement；待 21 的依赖状态核实后，按既有持续授权补齐可测试的 Agent Brief 和正式状态转换。
