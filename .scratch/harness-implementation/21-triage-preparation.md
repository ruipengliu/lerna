> *This was generated by AI during triage.*

# 21 票分诊预备记录

状态：预备记录已被正式分诊采纳。第 20 票在 df9d02d 完成验收后，21 已经 needs-triage → ready-for-agent 转换；权威执行要求以 [21 票 Agent Brief](issues/21-fetch.md) 为准。此文保留分诊准备时的调查记录。

## 核查结论

原票要求正式内容获取能力，属于 enhancement。已查阅共同工作包规格、领域词汇、工具执行/多模拟设备、记忆驻留、专项验收分册，以及现有 Execution、Content、Answer 和 HTTP Adapter。现有 `credentialhttp.Exit` 绑定单一服务/账户并只返回接受状态；`arkmodel.Model` 调用模型服务，两者均拒绝自动重定向且不交付网络来源证据。未找到实现通用网络内容获取、来源/时间/媒体类型记录及受控 Task Context 交付的现成能力，不能将已有 HTTP 传输认定为本票完成。仓库未发现 .out-of-scope 拒绝记录。

直接依赖仅 14，已完成其限定范围；18 真实模型质量未通过不阻塞本票的确定性获取验证。本票不用模型生成网页内容或以录制响应代替真实联网。22 负责搜索及答案综合，83 负责完整 V1 质量门槛，21 负责取得真实证据和有界失败。

## 拟采用的行为边界

- 用户任务通过正式已登记能力，沿 Core/Brain/Execution/SDK 的调用与授权机制进行获取；不另建独立执行循环或绕过任务预算的网络客户端。
- 获取依赖消费方接口。参考 HTTP Adapter 执行真实请求；固定重放实现只用于明确标记的契约测试。请求身份、原任务用量、失败、重启和取消沿已有机制保留。
- 输入包括获准目标和有限获取条件，宿主策略决定来源、处理位置、用途、尺寸、时间及保留上限；调用者只能收缩。URL/响应正文不能授予新来源、任意协议、凭证、保存或后台扫描权。
- 发出请求前及每次重定向重新校验有效目标；拒绝超限跳转、越权来源和不支持协议。网络地址解析与实际拨号遵守宿主网络范围，不能用重定向、代理或地址变化逃出允许范围。测试服务的本地地址须通过明确配置允许，不作为生产默认放行依据。
- 按原预算记录所有真实获取尝试和重定向；超时、取消、过大响应、媒体类型不支持、拒绝、来源不可访问和过期明确区分。连接失败或只取得部分字节不等于取得完整来源；禁止猜测缺失正文。无界重试、默认凭证转发和无限解压不在参考路径中。
- 成功证据保留请求目标、实际最终来源、获取时间、媒体类型、状态与受控正文/引用及可核对摘要；可判定的新鲜度与使用期限不得由网页自我声明提升。片段或转换需保留来源关联并声明处理方式，不将转换结果伪装成原始完整响应。
- 返回和使用出口复查当前权限及位置/用途限制。Task Context 材料保留来源及缺口，网页指令仅作不可信数据；结果不得自动写入长期 Memory，也不能泄漏未经允许的响应正文、敏感 URL 或头部到日志、错误和报告。
- 已取得的正文若需供任务重启恢复使用，按受控 Content 的保存授权和期限留存；缺少留存条件则明确不可恢复/需另行获准获取，不能伪造已持久结果或静默以新请求填充原证据时间。

## 拟采用的验收边界

1. 真实 Core/SDK 任务调用可替换获取器，将实际响应及元数据交付受控证据；后续 Task Context 使用保留来源与缺口，Memory 无自动新增记录。
2. 获权成功与拒绝反例覆盖原目标和重定向、处理位置/用途、当前授权撤销、尺寸/时间/类型限制及网页内指令；以实际网络服务收到的请求和实际受控内容核对效果。
3. 有界故障夹具覆盖超时、取消、拒绝、不可访问、过期、重定向循环/越界及超限正文，不能将失败或部分内容表示为完整成功；用量归属原任务且可核对。
4. 重复调用、原结果恢复和进程中断遵守既有操作身份，不重置预算、不把新的抓取结果或时间冒充原结果。真实响应、受控保存和确认间的退出窗口按具体实现验证。
5. 固定资料重放与公共来源真实联网分别具名运行；真实联网的小型记录包含配置、采集时间、来源、结果/失败、内容摘要及允许留存的证据。环境网络故障如实记录，不更换成全离线成功报告。
6. 提供 SDK/CLI/profile 入口和可替换 Adapter，实际版本/依赖/配置、预算、方法、结果及限制可复现；完成相关测试、全仓验证与双轴审查后方可标记本票完成。

不扩展为搜索排名、问答生成或完整 V1 质量评测；不要求任意网站/媒体格式、浏览器渲染、认证网站登录或全站爬虫。但这些边界不豁免真实网络获取、授权重定向、实际受控证据、任务预算及恢复验证。
