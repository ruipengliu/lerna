# 旧编码夹具

复用 06 票归档的 05 版本真实授权编码，来源提交 `37a51da`，SHA-256 `38719de0f52a20c5f5f6d8bbc8055ae854818215e61defebbb73587b44ce927f`。该文件没有 ContentData/ContentOperations 字段。公开测试凭证为 64 个 `a`，不对应真实账户。

验证将旧快照装入独立 SQLite，使用其原配置恢复身份和授权，新增内容授权与内容记录后核对旧授权和原管理回执。任务/控制的既有恢复由原 profile 回归提供证据。
