# Harness 领域词汇

本词汇表统一项目使用的领域用语。目标与能力验收要求见 [项目目标基线](docs/harness-project-goals.md)。

## Language

**Harness**：
连接记忆、大脑和执行三大系统的通用 Agent 运行与集成体系，是本开源项目的核心交付对象。
_Avoid_：将某个业务应用或某个模型本身称为 Harness。

**Harness 内核（Harness Core）**：
为记忆、大脑和执行协作提供公共运行保障的组成部分，承载任务生命周期与权限等跨系统责任。拥有 Harness 任务状态的推进权；大脑提出行动，执行系统报告事实，由内核据此管理任务进展。

**通用能力（Capability）**：
Harness 提供的一类可明确职责、输入输出与验收结果的行为，可由多个系统共同实现。
_Avoid_：将一项能力直接等同于一个系统、进程或部署节点。

**专项能力**：
由通用能力组合形成、仍须独立完成整体验收的对外行为；联网问答与手机 GUI 操作是本项目的强制专项能力。

**记忆系统（Memory）**：
承载用户个性化信息与可复用经验的系统，是三大逻辑系统之一。

**任务上下文（Task Context）**：
一次任务中为推理与执行选择和组织的工作信息，包括目标、进展、工具结果、相关证据与记忆。
_Avoid_：将进入任务上下文的信息直接当作已获授权保存的长期记忆。

**长期记忆（Long-term Memory）**：
在授权范围内保存、可跨任务复用的事实、偏好、推断与经验。

**大脑系统（Brain）**：
承担理解、规划与决策的系统，是三大逻辑系统之一。

**决策提案（Decision Proposal）**：
大脑依据当前任务信息提出的下一段有限工作或结果判断，包含行动、委派、等待或结果请求。提案被提出不代表工作已经获准执行或结果已经确认。

**执行系统（Execution）**：
承接行动请求并与工具、设备或外部系统交互的系统，是三大逻辑系统之一。

**运行节点（Node）**：
参与 Harness 协作、部署在端侧或云端的运行单元，其可提供的能力可以对外声明。
_Avoid_：将运行节点限定为手机，或将端与云直接当作固定的职责名称。

**模拟设备（Simulated Device）**：
通过软件状态提供设备观察和动作行为的验证对象，动作效果可依据状态变化核对，并可构造故障与用户接管条件。
_Avoid_：将固定返回成功的桩当作完整模拟设备，或将每个模拟设备等同于一个运行节点。

**任务（Task）**：
具有持久化状态、可以被分派并产生结果的工作单元，可覆盖一次问答或跨设备、多 Agent 的长时间执行。

**插件（Plugin）**：
为 Harness 提供可安装实现的扩展对象，例如记忆后端、模型适配器、搜索工具或设备驱动。

**扩展激活（Extension Activation）**：
将一个已验证且获准的扩展版本设为指定运行范围内可用于新工作的版本。不同节点的实际生效进展分别判断，既有工作保留其已选定版本。
_Avoid_：将内容导入、验证通过或管理请求受理等同于扩展已经生效。

**Skill**：
描述可复用操作知识与流程的扩展对象。
_Avoid_：将 Skill 与提供底层能力实现的插件混为同一对象。

**Agent**：
拥有目标、上下文和决策循环，并通过任务契约参与协作的执行主体。

**内部 Agent（Internal Agent）**：
生命周期由 Harness 管理、通过任务契约参与协作的 Agent。

**外部 Agent（External Agent）**：
拥有独立运行时、通过任务契约与 Harness 协作的 Agent。

**运行观测**：
关联跨端任务、工具调用、授权检查与结果记录，用于了解实际运行情况和定位问题的能力。

**能力评测**：
依据测试集与基线评价能力行为和效果，并测量质量、耗时与成本的能力。

**评测计划（Evaluation Plan）**：
预先固定一轮能力评测的对象、测试材料、判定规则、预算与比较条件的约定；它决定结果可以支持哪些结论。
_Avoid_：在看到结果后改变计划并仍将其当作同一轮验收。

**改进候选（Improvement Candidate）**：
针对已记录问题提出、具有明确产物版本与影响范围的改进对象，是否可以生效取决于相应证据和权限。
_Avoid_：将候选已生成、评测通过或发布请求受理直接等同于效果已改善。

**自进化（Self-evolution）**：
利用运行经验提出改进，并经过隔离评测、版本发布、效果监测与回滚机制改善系统能力的过程。
_Avoid_：将生成新内容直接等同于已完成自进化。

**操作（Operation）**：
一次具有稳定身份的逻辑变更意图，例如提交任务、执行能力、取消执行或更新记忆。一个任务可关联多个操作，独立操作也可不属于任务。
_Avoid_：将操作等同一次消息发送或一次数据库提交。

**授权记录（Grant）**：
有权签发者授予某个主体在指定资源、动作、用途、位置与期限内执行的权利记录，可进一步限制代理执行者及委派范围。
_Avoid_：将身份可信、持有第三方凭证或知道能力名称等同于拥有相应授权。

**能力目录（Capability Catalog）**：
组织 Harness 可发现能力及其适用条件和支持范围的信息集合，同一能力可以对应多个执行实现。
_Avoid_：将目录可见性等同执行授权，或把不同部署实例重复计为不同能力。

**资源接管（Resource Takeover）**：
用户取得某个受控设备或业务资源的操作控制，Harness 停止在该范围内启动冲突自动化动作，并处置在途动作的过程。
_Avoid_：将资源接管等同任务所有权移交，或将接管请求受理等同实际控制已经落实。

**能力调用（Capability Invocation）**：
一种执行类操作，身份在消息重发、执行进展和结果核对中保持。取消某次调用是引用该调用的另一项操作。
_Avoid_：将每次传输尝试都当作新的业务动作。

**持久化回执**：
接收方已保存消息及必要接收记录的确认，不代表动作已经执行、执行已获授权或效果已确认。

**效果确认**：
依据动作后的观察或可核对证据判断预期效果是否发生。
_Avoid_：把请求返回、远端完成状态或消息送达直接等同于效果确认。

**用户交互（User Interaction）**：
系统或接入应用与用户之间的呈现、输入和反馈过程，可关联任务，也可独立存在。
_Avoid_：将向用户展示内容等同用户已确认，或将端侧 UI 交互等同第三方应用的设备自动化操作。

**等待项（Pending Interaction）**：
由受控处理模块发起、具有明确输入要求和状态的待回应事项。输入操作引用具体等待项，已处理或失效的等待项不能被另一条迟到输入重新消费。

**任务拥有者（Task Owner）**：
唯一负责推进某个任务权威状态的逻辑责任方，可位于端侧或云侧；实际执行动作的位置可以不同。
_Avoid_：将一条连接、一个 UI 或一次 Worker 启动当作任务拥有者。

**受委派子任务（Delegated Subtask）**：
接收一段目标及自主运行责任、拥有独立状态的任务，与父任务通过委派和结果关系协作。
_Avoid_：为每次远程能力调用都额外创建一个子任务。

**所有权移交（Ownership Handoff）**：
将同一任务的状态推进责任从当前拥有者交给另一拥有者的受控过程，任务身份保持。移交前后不得存在两个有权推进该任务的拥有者。

**接纳窗口（Admission Window）**：
允许首次接收新操作的有限范围及期限。窗口关闭不取消已经受理的任务，也不授予执行权限；原操作不得仅因记录清理而被重新接纳。

**记忆记录（Memory Record）**：
具有稳定身份、明确类型和修订关系的长期记忆表达，携带来源、置信判断和适用范围。对同一条记忆的纠正保留其身份，不将不同来源的主张自动视为同一记录。

**记忆集合（Memory Collection）**：
共同管理修改权威与同步范围的一组记忆记录，可由端侧或云侧负责。集合归属不替代对具体记录和用途的授权。

**记忆修改权威（Memory Authority）**：
唯一负责将某个记忆集合的修改确认为正式修订的逻辑责任方。
_Avoid_：将持有同步副本的节点或任务拥有者自动当作记忆修改权威。

**记忆数据驻留（Memory Data Residency）**：
记忆及其来源、派生内容被允许保存的位置范围，与谁负责确认正式修订是不同约束。

**联合记忆检索（Federated Memory Retrieval）**：
在多个持有记忆的节点分别查询，再将允许返回的结果组织为可用信息的过程，不以汇集全部原始数据为前提。

**端侧记忆挖掘（On-device Memory Extraction）**：
在端侧从获准访问的信息中提取可复用的事实、偏好、推断或经验的过程。提取出的内容仍须满足保存、用途与跨端传递的约束。
